services:
  vexor:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app
      - /app/venv
    restart: unless-stopped
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload